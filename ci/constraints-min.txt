# Exact floor pins for the CI "min-deps" lane — the oldest supported release
# of each dependency (matches the >= floors in pyproject.toml and
# gui/backend/requirements.txt). All support Python 3.11 and NumPy 2.
numpy==2.4.6
scipy==1.13.1
matplotlib==3.9.4
pandas==2.2.3
scikit-learn==1.5.2
fastapi==0.110.3
uvicorn==0.23.2
pydantic==2.5.3
pytest==7.4.4
