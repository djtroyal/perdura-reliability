# Source-install lower bounds describe the APIs used by the backend. Perdura's
# release binaries bundle the current dependency set exercised by release CI.
fastapi>=0.139.2
uvicorn[standard]>=0.23
pydantic>=2.5
numpy>=2.0
scipy>=1.13
pandas>=2.2.2
scikit-learn>=1.5
# Safe, portable fitted-model export and bounded local inference.  Perdura does
# not load pickle/joblib artifacts.
onnx>=1.17
onnxruntime>=1.19
skl2onnx>=1.17
